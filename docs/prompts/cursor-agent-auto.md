# Cursor Agent Auto 用プロンプト集

## A. 新機能実装（仕様→実装→テスト）

```
あなたは本リポの実装担当エージェント。目的は「featuresRegistry に定義された <機能ID> を本番品質で実装し、E2Eまで通す」こと。

要件:
- 仕様の一次情報は `src/config/featuresRegistry.ts`。必要なら読み込み、自分で差分を出す
- UIは MobileHeader を使用し、iPhone SE 幅で崩れないこと
- 既存 hooks/api 以外へ新規APIは追加しない（まずローカル状態で成立させる）
- ESLint/TypeScript エラー 0
- `pnpm run test:ci` を通すテストを追加（最低1）
- 既存の `status:build` が落ちないように CLS 0 配慮

出力:
1) 変更ファイル一覧（追加/更新/削除）
2) 主要コード差分 (必要部分のみ)
3) テストコード
4) リスクと簡易ロールバック案
5) 実行コマンド

開始: <機能IDの簡約仕様> を読み取り、実装計画→着手→差分提示の順で。
```

## B. 既存コードの安全リファクタ

```
目的: <対象ファイル/ディレクトリ> の可読性改善＋副作用削減。外部API/型Public Surfaceは変えない。

制約:
- 1PR 300行以内の差分
- 型安全性↑、any撲滅、早期return、pure関数化
- 既存テストは壊さない。足りなければ追加
- パフォーマンスが悪化する変更はしない

出力: 差分、動作確認手順、測定/リグレッションリスク
```

## C. バグ再現→最短修正

```
現象: <スクショ/ログ/再現手順>
期待値と実際: <…>
制約: 変更は最小、E2Eを1つ追加、AnalyticsやSentryへのログを仕込む

出力: 原因仮説→再現コード→修正差分→テスト→計測方法
```

## D. PR本文自動生成

```
この差分から PR の本文を生成。PULL_REQUEST_TEMPLATE に完全準拠し、プレビューURLの項目を残す。
```
