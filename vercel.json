{
  "buildCommand": "pnpm run vercel-build",
  "outputDirectory": "dist",
  "framework": null,
  "installCommand": "corepack enable && corepack prepare pnpm@8.15.9 --activate || npm i -g pnpm@8.15.9 && pnpm install --frozen-lockfile",
  "devCommand": "pnpm run dev",
  "functions": {
    "api/**/*.ts": {
      "includeFiles": "src/{services,server,types,utils}/**",
      "excludeFiles": "{src/components/**,src/pages/**,coverage/**,**/__tests__/**}"
    }
  },
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}